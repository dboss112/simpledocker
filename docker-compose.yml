version: '2.0'
services:
  database:
    build: ./db_docker
    environment:
     - MYSQL_ROOT_PASSWORD=swarup
     - jdbc.driverClassName=com.mysql.jdbc.Driver
     - jdbc.url=jdbc:mysql://database/account
     - jdbc.username=root
     - jdbc.password=swarup
    ports:
     - '3306:3306'
  web:
    build: .
    ports:
     - '8009:8080'
    links:
     - database
    
    
